import {MateTimepickerFace} from './timepicker-face.directive';

describe('MateTimepickerFace', () => {
    it('should create an instance', () => {
        const directive = new MateTimepickerFace();
        expect(directive).toBeTruthy();
    });
});
